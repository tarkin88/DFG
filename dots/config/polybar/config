;=====================================================
;     File Includes
;=====================================================

[section/base]
include-file = ~/.config/polybar/master.conf
include-file = ~/.config/polybar/modules.conf

[bar/status_bar]
inherit = bar/master

; Use $ xrandr -q | grep " connected" | cut -d ' ' -f1
monitor = MONITOR


modules-left   = clock mpd spotify
modules-center = i3 
MODULES_RIGHT
;modules-right  = pkg toggle_xautlock memory coreuse wireless-network volume

tray-position = right
tray-padding = 0
tray-maxsize = 16
